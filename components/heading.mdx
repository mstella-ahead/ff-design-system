---
name: Heading
component: heading
instances: 124
variants: 4
pages: [applications-case-studies, blog, blog-2026-why-semiconductor-test-infrastructure-matters-more-than-ever, company-about-us, company-careers-career-opportunities, company-news-events-press-releases, events, home, industries-automotive, press-release-formfactor-introduces-the-evolvity-300-probe-system, product-probe-cards-foundry-logic-apollo, products, products-probe-cards, sales-service-contact-sales, technologies-contact-intelligence]
---

# Heading

Section and page titles. Teal at every level — this is the single strongest brand rule on the site.

**When to use:** Choose the level to match the document outline, never for visual size alone. Do not recolor: h1–h4 are teal --secondary in every template observed.

Observed **124** time(s) across **15** page(s).

## Variants

| Variant | Background | Text | Radius | Count | Pages | Example classes |
|---|---|---|---|---:|---:|---|
| `h3` | — | `#003a63` (`primary`) | 0px | 51 | 10 | `product-name` |
| `h2` | — | `#00a0af` (`secondary`) | 0px | 39 | 8 | `(h2, no class)` |
| `h4` | — | `#003a63` (`primary`) | 0px | 20 | 4 | `heading` |
| `h1` | — | `#00a0af` (`secondary`) | 0px | 14 | 14 | `page-header__heading` |

## Rules and gotchas

- **Teal `--secondary` is the default heading color, but it is not universal — the level matters.** Measured across the crawl: `h1` is teal in **14/14** instances (an absolute rule); `h2` is teal 32 / white 7 (white only on dark bands) and never navy; `h3` is teal 41 / navy 33; `h4` is navy 12 / teal 4 / grey 3.
- **The navy ones are card titles.** Every navy `h3.heading`/`h4.heading` sits inside a card or CTA item (`li.cta-card`, `ul.cta-grid`, `article.featured-post`), and most are wrapped in an `<a>` — so they inherit the anchor's navy link color rather than being recolored deliberately. Rule of thumb: a heading that *is* the clickable title of a card renders navy; a heading that labels a section renders teal.
- Weight is 400 at every level — the size carries the hierarchy, not the weight.
- Heading color does not change responsively; only size does. The colors above are identical at 1440px and 390px.
- Sizes come from the modular scale with a per-level multiplier, e.g. base `h2 { font-size: calc(var(--size-600) * 1.2rem) }`, escalating a step or two at each breakpoint. See the responsive section of `../tokens/REPORT.md`.

## Canonical markup

Real theme class names, so a prototype inherits the theme CSS. Imagery is a placeholder — FormFactor's own photography and logos are never redistributed from this repo.

```html
<h1 class="page-header__heading">Page title</h1>  <!-- 67.2px teal, always -->
<h2>Section heading</h2>                           <!-- 45.3px teal -->
<h3>Subsection</h3>                                <!-- 30.0px teal -->

<!-- Inside a card the title is a link, so it reads navy: -->
<li class="cta-card"><a href="/x"><h4 class="heading">Card title</h4></a></li>
```

## Observed instance

The most representative on-canvas instance, for cross-checking against the screenshots.

- **Screenshot:** `raw/products-probe-cards/screenshot.png` — region x:525 y:1112 w:421 h:30
- **Classes:** `product-name`
- **Key styles:** color rgb(0, 58, 99); background rgba(0, 0, 0, 0); font 30.0192px/400; padding 0px 0px 0px 0px; radius 0px

## Source pages

- `raw/applications-case-studies/`
- `raw/blog/`
- `raw/blog-2026-why-semiconductor-test-infrastructure-matters-more-than-ever/`
- `raw/company-about-us/`
- `raw/company-careers-career-opportunities/`
- `raw/company-news-events-press-releases/`
- `raw/events/`
- `raw/home/`
- `raw/industries-automotive/`
- `raw/press-release-formfactor-introduces-the-evolvity-300-probe-system/`
- `raw/product-probe-cards-foundry-logic-apollo/`
- `raw/products/`
- `raw/products-probe-cards/`
- `raw/sales-service-contact-sales/`
- `raw/technologies-contact-intelligence/`
